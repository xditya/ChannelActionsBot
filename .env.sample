BOT_TOKEN=
OWNERS=
MONGO_URL=
# Optional: port for the web dashboard + webhook server (default 8000)
PORT=
# Optional: public HTTPS URL of the dashboard, e.g. https://bot.example.com
# When set, it is installed as the bot's menu button (Telegram Mini App).
WEBAPP_URL=
